import { css } from 'lit';

export const borderRadiusTokens = css`
  :root {
    --radius-s: 0.25rem;
    --radius-m: 0.5rem;
    --radius-l: 1rem;
    --radius-pill: 999px;
    --radius-circle: 50%;
  }
`;
