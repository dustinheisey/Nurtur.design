import { Meta } from '@storybook/blocks'
import { DesignTokenDocBlock } from 'storybook-design-token'

<Meta title="Tokens/Border Radius" />

## Border Radius

- Using `rem` for border-radius makes sense because larger elements like cards get a larger border radius than buttons.
- When a user increases their font size, the buttons and cards get bigger, so the border radius should increase proportionally.

<DesignTokenDocBlock categoryName="BorderRadius" />
