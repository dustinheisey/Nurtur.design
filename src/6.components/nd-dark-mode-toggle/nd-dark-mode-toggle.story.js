import { html } from 'lit'
import './nd-dark-mode-toggle.js'

export default {
  title: 'Components/Dark Mode Toggle',
}

const Template = () => html` <nd-dark-mode-toggle></nd-dark-mode-toggle> `

export const Default = Template.bind({})
